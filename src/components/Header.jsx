import {Logo} from "./Logo.jsx";
import {Counter} from "./Counter.jsx";

export const Header = () => {
    return (
        <header>
            <Logo />
            <Counter />
        </header>
    )
}